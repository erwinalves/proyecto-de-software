<%= form_for @carro, url: carros_path, html: { method: :post} do |f| %>
  <% if @carro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carro.errors.count, "error") %> prohibited this carro from being saved:</h2>

      <ul>
      <% @carro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_Producto %><br>
    <%= f.number_field :product_id%>
  </div>
  <div class="field">
    <%= f.label :id_Boleta %><br>
    <%= f.number_field :ticket_id %>
  </div>
  <div class="field">
    <%= f.label :cantidad_producto %><br>
    <%= f.number_field :cantidad_producto %>
  </div>
  <div class="actions">
    <%= f.submit "Crear" %>
  </div>
<% end %>
