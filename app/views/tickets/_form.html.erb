<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo_boleta %><br>
    <%= f.number_field :codigo_boleta %>
  </div>
  <div class="field">
    <%= f.label :monto %><br>
    <%= f.text_field :monto %>
  </div>
  <div class="field">
    <%= f.label :tipo_pago %><br>
    <%= options_for_select([['Efectivo (ingrese un 1)',1], ['Debito (ingrese un 2)', 2], ['Credito (ingrese un 3)', 3],['Cheque (ingrese un 4)', 4]]) %>
    <%= f.number_field :tipo_pago %>
  </div>
  <div class="actions">
    <%= f.submit "Crear"%>
  </div>
<% end %>
