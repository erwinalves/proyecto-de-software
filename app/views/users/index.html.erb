<%@users.each do |users| %>
<% users.id %>
<%end%>

<p id="notice"><%= notice %></p>

<h1>Lista de personas</h1>

<table>
  <thead>
    <tr>
      <th>Nombre del persona</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to 'Mostrar', user %></td>
        <td><%= link_to 'Eliminar', user, method: :delete, data: { confirm: '¿esta seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva persona', new_user_path %>